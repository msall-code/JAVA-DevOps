spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/microservices_db
    username: postgres
    password: MonNouveauMotDePasse123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:7080/realms/microservices-realm

server:
  port: 8081